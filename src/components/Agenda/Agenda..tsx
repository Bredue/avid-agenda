// check if user has tasks set for the current week
// if not, ask user to create classes/tasks

import React from "react";

const App = () => {
  return (
    <p>
      App
    </p>
  )
};

export default App;